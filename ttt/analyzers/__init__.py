"""
ttt.analyzers — Internal analysis utilities

This package contains code-level analyzers used internally by the conversion
pipeline. These modules are NOT exposed in the CLI and are not part of the
public API.

For server-level analysis (stats, dead_code, duplicates, storage, etc.),
see ttt.analyzer instead.
"""
